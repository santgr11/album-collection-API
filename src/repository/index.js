const albumsRepository = require('./albums');
const artistsRepository = require('./artists');
const tracksRepository = require('./tracks');

module.exports = {
  albumsRepository,
  artistsRepository,
  tracksRepository
};
